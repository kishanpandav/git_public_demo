git config user.email ""
git config user.name ""
git clone repo url(https://ACCESS_TOKEN@github.com/deniapp.git)
git add specific folder/files or . (will add all change in stage)
git commit -m "message"
git push origin branch_name
git checkout -b branch_name // create branch
git checkout brach_name/commit_hash // switch branch

git stash
git stash pop